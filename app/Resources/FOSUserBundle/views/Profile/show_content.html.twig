<div class="col-70-auto">
    <div class="form-horizontal fos_user_user_show">
        <br/>
        <div class="form-group">
            <div class="col-md-2">{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}:</div>
            <div class="col-md-6">{{ user.username }}</div>
        </div>
        <div class="form-group">
            <div class="col-md-2">{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}:</div>
            <div class="col-md-6">{{ user.email }}</div>
        </div>
    </div>
</div>
